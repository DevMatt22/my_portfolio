name: CI
on: push
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: git fetch --no-tags --prune --depth=1 origin dev
      - run: npm install
      - run: npx nx affected --target=test --base=origin/dev
      - run: npx nx affected --target=lint --base=origin/dev
      - run: npx nx affected --target=build --base=origin/dev
